{
  "name": "maulocum-messaging",
  "private": true,
  "packageManager": "bun@1.3.0",
  "workspaces": [
    "apps/*",
    "lib"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "worker": "turbo run worker",
    "lint": "bunx biome check --write ./apps/http/src ./apps/worker/src"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "turbo": "^2.7.2"
  }
}